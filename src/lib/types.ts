interface LoadedPair {
  prKey: string;
  pbKey: string;
  RC: string;
  uniqueString: string;
}
interface IKeyPairs {
  prKey: string;
  pbKey: string;
  RC: string;
  uniqueString: string;
}

export type { LoadedPair, IKeyPairs };
